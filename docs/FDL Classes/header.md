# Header

The FDL header contains metadata about the document: UUID, schema version,
creator information, and the default framing intent reference. Header fields
are accessible directly on the `FDL` object.

::: fdl.Version
    options:
        inherited_members: false
